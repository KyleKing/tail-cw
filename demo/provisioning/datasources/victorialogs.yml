apiVersion: 1

datasources:
  - name: VictoriaLogs
    type: victoriametrics-logs-datasource
    access: proxy
    url: http://victorialogs:9428
    isDefault: true
    editable: true
    jsonData:
      # Optional: configure derived fields for trace correlation
      # derivedFields:
      #   - name: TraceID
      #     matcherRegex: '"trace_id":\s*"([^"]+)"'
      #     url: ""
