"""Textual views: the group browser, the log table, dashboards, and the reports.

Import the modules directly. A facade here would load Textual for a caller that
only wanted to format a timestamp.
"""
